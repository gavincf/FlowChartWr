
WAITKEY_CHK_PCC_RANDOM EQU C_TRUE
.IF(WAITKEY_CHK_PCC_RANDOM.EQ.C_FALSE)
	.IF ( C_RANDOM0_EN .EQ. 1 )
	INC	<RANDOM0
	.ENDIF

.ENDIF;WAITKEY_CHK_PCC_RANDOM

WAITKEY_FXF15_PCC_RANDOM EQU C_TRUE
.IF(WAITKEY_FXF15_PCC_RANDOM.EQ.C_FALSE)
	.IF ( C_RANDOM1_EN .EQ. 1 )
	INC	<RANDOM1
	.ENDIF

.ENDIF;WAITKEY_FXF15_PCC_RANDOM

POR_MODULE_PCC_RANDOM EQU C_TRUE
.IF(POR_MODULE_PCC_RANDOM.EQ.C_FALSE)
	.IF ( C_RANDOM_GP_USED .EQ. 1)
	JSR	RANDOM_GP_BUF_INIT
	.ENDIF
.ENDIF;POR_MODULE_PCC_RANDOM


RANDOM_GP_BUF_INIT:
	LDX	#0
RANDOM_GP_BUF_INIT_S10:
	LDA	#0FFH
	STA	R_RANDOM_GP_BUF,X	
	INX
	CPX	#C_RANDOM_GP_CNT
	BCC	RANDOM_GP_BUF_INIT_S10
	
	RTS
