
;--------------------------------------
;>>>> 	configure    <<<<
;--------------------------------------
;关于扫描按键通道和扫描状态
;
;关于扫描按键通道:
; 是一个触发状态，有对应的事件处理：
;
;关于扫描状态:
; 更新状态，记录状态，不一定需要处理
;
;
;--------------------------------
;	input port
;-------------------------------

;=========================================
;=========================================
.if (CHIPSERIES = 'N588H') .OR. (CHIPSERIES = 'N588J') .OR. (CHIPSERIES = 'N567G') .OR. (CHIPSERIES = 'N567H') .OR. (CHIPSERIES = 'N567K') .OR. (CHIPSERIES = 'N588L')

;debounce * 5.33ms
.IF ( C_SKEY0_EN .EQ. 1)
SKEY0_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY0	EQU	C_BIT0
SR_SKEY0_PORT	EQU	BP1
R_SKEY0_STATUS	EQU	BP1_STATUS
C_SKEY0_DEB_F	EQU	7H;CNT
C_SKEY0_DEB_R	EQU	1H;CNT
R_SKEY0_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY1_EN .EQ. 1)
SKEY1_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY1	EQU	C_BIT2
SR_SKEY1_PORT	EQU	BP1
R_SKEY1_STATUS	EQU	BP1_STATUS
C_SKEY1_DEB_F	EQU	7H;CNT
C_SKEY1_DEB_R	EQU	1H;CNT
R_SKEY1_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
	
.ENDIF
.IF ( C_SKEY2_EN .EQ. 1)
SKEY2_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY2	EQU	C_BIT2
SR_SKEY2_PORT	EQU	BP1
R_SKEY2_STATUS	EQU	BP1_STATUS
C_SKEY2_DEB_F	EQU	7H;CNT
C_SKEY2_DEB_R	EQU	1H;CNT
R_SKEY2_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY3_EN .EQ. 1)
SKEY3_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY3	EQU	C_BIT3
SR_SKEY3_PORT	EQU	BP1
R_SKEY3_STATUS	EQU	BP1_STATUS
C_SKEY3_DEB_F	EQU	7H;CNT
C_SKEY3_DEB_R	EQU	1H;CNT
R_SKEY3_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY4_EN .EQ. 1)
SKEY4_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY4	EQU	C_BIT4
SR_SKEY4_PORT	EQU	BP1
R_SKEY4_STATUS	EQU	BP1_STATUS
C_SKEY4_DEB_F	EQU	7H;CNT
C_SKEY4_DEB_R	EQU	1H;CNT
R_SKEY4_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY5_EN .EQ. 1)
SKEY5_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY5	EQU	C_BIT5
SR_SKEY5_PORT	EQU	BP1
R_SKEY5_STATUS	EQU	BP1_STATUS
C_SKEY5_DEB_F	EQU	7H;CNT
C_SKEY5_DEB_R	EQU	1H;CNT
R_SKEY5_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY6_EN .EQ. 1)
SKEY6_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY6	EQU	C_BIT6
SR_SKEY6_PORT	EQU	BP1
R_SKEY6_STATUS	EQU	BP1_STATUS
C_SKEY6_DEB_F	EQU	7H;CNT
C_SKEY6_DEB_R	EQU	1H;CNT
R_SKEY6_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
;
.IF ( C_SKEY7_EN .EQ. 1)
SKEY7_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY7	EQU	C_BIT7
SR_SKEY7_PORT	EQU	BP1
R_SKEY7_STATUS	EQU	BP1_STATUS
C_SKEY7_DEB_F	EQU	7H;CNT
C_SKEY7_DEB_R	EQU	1H;CNT
R_SKEY7_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY8_EN .EQ. 1)
SKEY8_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY8	EQU	C_BIT7
SR_SKEY8_PORT	EQU	BP0
R_SKEY8_STATUS	EQU	BP0_STATUS
C_SKEY8_DEB_F	EQU	7H;CNT
C_SKEY8_DEB_R	EQU	1H;CNT
R_SKEY8_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY9_EN .EQ. 1)
SKEY9_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY9	EQU	C_BIT6
SR_SKEY9_PORT	EQU	BP1
R_SKEY9_STATUS	EQU	BP1_STATUS
C_SKEY9_DEB_F	EQU	7H;CNT
C_SKEY9_DEB_R	EQU	1H;CNT
R_SKEY9_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY10_EN .EQ. 1)
SKEY10_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY10	EQU	C_BIT7
SR_SKEY10_PORT	EQU	BP1
R_SKEY10_STATUS	EQU	BP1_STATUS
C_SKEY10_DEB_F	EQU	7H;CNT
C_SKEY10_DEB_R	EQU	1H;CNT
R_SKEY10_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY11_EN .EQ. 1)
SKEY11_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY11	EQU	C_BIT7
SR_SKEY11_PORT	EQU	BP0
R_SKEY11_STATUS	EQU	BP0_STATUS
C_SKEY11_DEB_F	EQU	7H;CNT
C_SKEY11_DEB_R	EQU	1H;CNT
R_SKEY11_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY12_EN .EQ. 1)
SKEY12_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY12	EQU	C_BIT2
SR_SKEY12_PORT	EQU	BP1
R_SKEY12_STATUS	EQU	BP1_STATUS
C_SKEY12_DEB_F	EQU	7H;CNT
C_SKEY12_DEB_R	EQU	1H;CNT
R_SKEY12_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY13_EN .EQ. 1)
SKEY13_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY13	EQU	C_BIT2
SR_SKEY13_PORT	EQU	BP1
R_SKEY13_STATUS	EQU	BP1_STATUS
C_SKEY13_DEB_F	EQU	7H;CNT
C_SKEY13_DEB_R	EQU	1H;CNT
R_SKEY13_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY14_EN .EQ. 1)
SKEY14_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY14	EQU	C_BIT2
SR_SKEY14_PORT	EQU	BP1
R_SKEY14_STATUS	EQU	BP1_STATUS
C_SKEY14_DEB_F	EQU	7H;CNT
C_SKEY14_DEB_R	EQU	1H;CNT
R_SKEY14_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY15_EN .EQ. 1)
SKEY15_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY15	EQU	C_BIT2
SR_SKEY15_PORT	EQU	BP1
R_SKEY15_STATUS	EQU	BP1_STATUS
C_SKEY15_DEB_F	EQU	7H;CNT
C_SKEY15_DEB_R	EQU	1H;CNT
R_SKEY15_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY16_EN .EQ. 1)
SKEY16_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY16	EQU	C_BIT2
SR_SKEY16_PORT	EQU	BP1
R_SKEY16_STATUS	EQU	BP1_STATUS
C_SKEY16_DEB_F	EQU	7H;CNT
C_SKEY16_DEB_R	EQU	1H;CNT
R_SKEY16_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY17_EN .EQ. 1)
SKEY17_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY17	EQU	C_BIT2
SR_SKEY17_PORT	EQU	BP1
R_SKEY17_STATUS	EQU	BP1_STATUS
C_SKEY17_DEB_F	EQU	7H;CNT
C_SKEY17_DEB_R	EQU	1H;CNT
R_SKEY17_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY18_EN .EQ. 1)
SKEY18_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY18	EQU	C_BIT2
SR_SKEY18_PORT	EQU	BP1
R_SKEY18_STATUS	EQU	BP1_STATUS
C_SKEY18_DEB_F	EQU	7H;CNT
C_SKEY18_DEB_R	EQU	1H;CNT
R_SKEY18_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY19_EN .EQ. 1)
SKEY19_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY19	EQU	C_BIT2
SR_SKEY19_PORT	EQU	BP1
R_SKEY19_STATUS	EQU	BP1_STATUS
C_SKEY19_DEB_F	EQU	7H;CNT
C_SKEY19_DEB_R	EQU	1H;CNT
R_SKEY19_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY20_EN .EQ. 1)
SKEY20_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY20	EQU	C_BIT2
SR_SKEY20_PORT	EQU	BP1
R_SKEY20_STATUS	EQU	BP1_STATUS
C_SKEY20_DEB_F	EQU	7H;CNT
C_SKEY20_DEB_R	EQU	1H;CNT
R_SKEY20_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY21_EN .EQ. 1)
SKEY21_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY21	EQU	C_BIT2
SR_SKEY21_PORT	EQU	BP1
R_SKEY21_STATUS	EQU	BP1_STATUS
C_SKEY21_DEB_F	EQU	7H;CNT
C_SKEY21_DEB_R	EQU	1H;CNT
R_SKEY21_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY22_EN .EQ. 1)
SKEY22_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY22	EQU	C_BIT2
SR_SKEY22_PORT	EQU	BP1
R_SKEY22_STATUS	EQU	BP1_STATUS
C_SKEY22_DEB_F	EQU	7H;CNT
C_SKEY22_DEB_R	EQU	1H;CNT
R_SKEY22_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY23_EN .EQ. 1)
SKEY23_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY23	EQU	C_BIT2
SR_SKEY23_PORT	EQU	BP1
R_SKEY23_STATUS	EQU	BP1_STATUS
C_SKEY23_DEB_F	EQU	7H;CNT
C_SKEY23_DEB_R	EQU	1H;CNT
R_SKEY23_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.ENDIF

;=========================================
  .if (CHIPSERIES = 'N589A') .OR. (CHIPSERIES = 'N589B') .OR. (CHIPSERIES = 'N589C') .OR. (CHIPSERIES = 'N589D')
;debounce * 3.2MS
;GAP2:12.88MS
.IF ( C_SKEY0_EN .EQ. 1)
SKEY0_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY0	EQU	C_BIT3
SR_SKEY0_PORT	EQU	BP1R
R_SKEY0_STATUS	EQU	BP1_STATUS
C_SKEY0_DEB_F	EQU	0AH;CNT
C_SKEY0_DEB_R	EQU	1H;CNT
R_SKEY0_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY1_EN .EQ. 1)
SKEY1_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY1	EQU	C_BIT5
SR_SKEY1_PORT	EQU	BP1R
R_SKEY1_STATUS	EQU	BP1_STATUS
C_SKEY1_DEB_F	EQU	0AH;CNT
C_SKEY1_DEB_R	EQU	1H;CNT
R_SKEY1_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
	
.ENDIF
.IF ( C_SKEY2_EN .EQ. 1)
SKEY2_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY2	EQU	C_BIT2
SR_SKEY2_PORT	EQU	BP1R
R_SKEY2_STATUS	EQU	BP1_STATUS
C_SKEY2_DEB_F	EQU	0AH;CNT
C_SKEY2_DEB_R	EQU	1H;CNT
R_SKEY2_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY3_EN .EQ. 1)
SKEY3_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY3	EQU	C_BIT7
SR_SKEY3_PORT	EQU	BP1R
R_SKEY3_STATUS	EQU	BP1_STATUS
C_SKEY3_DEB_F	EQU	0AH;CNT
C_SKEY3_DEB_R	EQU	1H;CNT
R_SKEY3_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY4_EN .EQ. 1)
SKEY4_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY4	EQU	C_BIT4
SR_SKEY4_PORT	EQU	BP1R
R_SKEY4_STATUS	EQU	BP1_STATUS
C_SKEY4_DEB_F	EQU	0AH;CNT
C_SKEY4_DEB_R	EQU	1H;CNT
R_SKEY4_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY5_EN .EQ. 1)
SKEY5_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY5	EQU	C_BIT5
SR_SKEY5_PORT	EQU	BP1R
R_SKEY5_STATUS	EQU	BP1_STATUS
C_SKEY5_DEB_F	EQU	0AH;CNT
C_SKEY5_DEB_R	EQU	1H;CNT
R_SKEY5_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
.IF ( C_SKEY6_EN .EQ. 1)
SKEY6_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY6	EQU	C_BIT6
SR_SKEY6_PORT	EQU	BP1R
R_SKEY6_STATUS	EQU	BP1_STATUS
C_SKEY6_DEB_F	EQU	0AH;CNT
C_SKEY6_DEB_R	EQU	1H;CNT
R_SKEY6_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF
;
.IF ( C_SKEY7_EN .EQ. 1)
SKEY7_FLAG	EQU	DP_SKEY_FLAG0
B_SKEY7	EQU	C_BIT7
SR_SKEY7_PORT	EQU	BP1R
R_SKEY7_STATUS	EQU	BP1_STATUS
C_SKEY7_DEB_F	EQU	0AH;CNTBP1_STATUS
C_SKEY7_DEB_R	EQU	1H;CNT
R_SKEY7_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY8_EN .EQ. 1)
SKEY8_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY8	EQU	C_BIT7
SR_SKEY8_PORT	EQU	BP0
R_SKEY8_STATUS	EQU	BP0_STATUS
C_SKEY8_DEB_F	EQU	0AH;CNT
C_SKEY8_DEB_R	EQU	1H;CNT
R_SKEY8_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY9_EN .EQ. 1)
SKEY9_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY9	EQU	C_BIT6
SR_SKEY9_PORT	EQU	BP1R
R_SKEY9_STATUS	EQU	BP1_STATUS
C_SKEY9_DEB_F	EQU	0AH;CNT
C_SKEY9_DEB_R	EQU	1H;CNT
R_SKEY9_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY10_EN .EQ. 1)
SKEY10_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY10	EQU	C_BIT7
SR_SKEY10_PORT	EQU	BP1R
R_SKEY10_STATUS	EQU	BP1_STATUS
C_SKEY10_DEB_F	EQU	0AH;CNT
C_SKEY10_DEB_R	EQU	1H;CNT
R_SKEY10_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY11_EN .EQ. 1)
SKEY11_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY11	EQU	C_BIT7
SR_SKEY11_PORT	EQU	BP0
R_SKEY11_STATUS	EQU	BP0_STATUS
C_SKEY11_DEB_F	EQU	0AH;CNT
C_SKEY11_DEB_R	EQU	1H;CNT
R_SKEY11_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY12_EN .EQ. 1)
SKEY12_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY12	EQU	C_BIT2
SR_SKEY12_PORT	EQU	BP1R
R_SKEY12_STATUS	EQU	BP1_STATUS
C_SKEY12_DEB_F	EQU	0AH;CNT
C_SKEY12_DEB_R	EQU	1H;CNT
R_SKEY12_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY13_EN .EQ. 1)
SKEY13_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY13	EQU	C_BIT2
SR_SKEY13_PORT	EQU	BP1R
R_SKEY13_STATUS	EQU	BP1_STATUS
C_SKEY13_DEB_F	EQU	0AH;CNT
C_SKEY13_DEB_R	EQU	1H;CNT
R_SKEY13_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY14_EN .EQ. 1)
SKEY14_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY14	EQU	C_BIT2
SR_SKEY14_PORT	EQU	BP1R
R_SKEY14_STATUS	EQU	BP1_STATUS
C_SKEY14_DEB_F	EQU	0AH;CNT
C_SKEY14_DEB_R	EQU	1H;CNT
R_SKEY14_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY15_EN .EQ. 1)
SKEY15_FLAG	EQU	DP_SKEY_FLAG1
B_SKEY15	EQU	C_BIT2
SR_SKEY15_PORT	EQU	BP1R
R_SKEY15_STATUS	EQU	BP1_STATUS
C_SKEY15_DEB_F	EQU	0AH;CNT
C_SKEY15_DEB_R	EQU	1H;CNT
R_SKEY15_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY16_EN .EQ. 1)
SKEY16_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY16	EQU	C_BIT2
SR_SKEY16_PORT	EQU	BP1R
R_SKEY16_STATUS	EQU	BP1_STATUS
C_SKEY16_DEB_F	EQU	0AH;CNT
C_SKEY16_DEB_R	EQU	1H;CNT
R_SKEY16_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY17_EN .EQ. 1)
SKEY17_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY17	EQU	C_BIT2
SR_SKEY17_PORT	EQU	BP1R
R_SKEY17_STATUS	EQU	BP1_STATUS
C_SKEY17_DEB_F	EQU	0AH;CNT
C_SKEY17_DEB_R	EQU	1H;CNT
R_SKEY17_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY18_EN .EQ. 1)
SKEY18_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY18	EQU	C_BIT2
SR_SKEY18_PORT	EQU	BP1R
R_SKEY18_STATUS	EQU	BP1_STATUS
C_SKEY18_DEB_F	EQU	0AH;CNT
C_SKEY18_DEB_R	EQU	1H;CNT
R_SKEY18_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY19_EN .EQ. 1)
SKEY19_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY19	EQU	C_BIT2
SR_SKEY19_PORT	EQU	BP1R
R_SKEY19_STATUS	EQU	BP1_STATUS
C_SKEY19_DEB_F	EQU	0AH;CNT
C_SKEY19_DEB_R	EQU	1H;CNT
R_SKEY19_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY20_EN .EQ. 1)
SKEY20_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY20	EQU	C_BIT2
SR_SKEY20_PORT	EQU	BP1R
R_SKEY20_STATUS	EQU	BP1_STATUS
C_SKEY20_DEB_F	EQU	0AH;CNT
C_SKEY20_DEB_R	EQU	1H;CNT
R_SKEY20_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY21_EN .EQ. 1)
SKEY21_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY21	EQU	C_BIT2
SR_SKEY21_PORT	EQU	BP1R
R_SKEY21_STATUS	EQU	BP1_STATUS
C_SKEY21_DEB_F	EQU	0AH;CNT
C_SKEY21_DEB_R	EQU	1H;CNT
R_SKEY21_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY22_EN .EQ. 1)
SKEY22_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY22	EQU	C_BIT2
SR_SKEY22_PORT	EQU	BP1R
R_SKEY22_STATUS	EQU	BP1_STATUS
C_SKEY22_DEB_F	EQU	0AH;CNT
C_SKEY22_DEB_R	EQU	1H;CNT
R_SKEY22_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.IF ( C_SKEY23_EN .EQ. 1)
SKEY23_FLAG	EQU	DP_SKEY_FLAG2
B_SKEY23	EQU	C_BIT2
SR_SKEY23_PORT	EQU	BP1R
R_SKEY23_STATUS	EQU	BP1_STATUS
C_SKEY23_DEB_F	EQU	0AH;CNT
C_SKEY23_DEB_R	EQU	1H;CNT
R_SKEY23_DEB	DS	1
;.7=0:for fall
;.7=1:for rise
.ENDIF

.ENDIF

.IF ( C_SKEY0DUTY_EN .EQ. 1 )
;for saving ram
;1ms,*8 = 8ms
R_SKEY0DUTY_FG DS	(C_SKEY0DUTY_BUF_BIT/8)
;each bit
;
R_SKEY0DUTY_CNT DS 1
;.2:.0: count *8
;.7:.4: count 32 bit
R_SKEY0DUTY_L_CNT DS	1
;.3:.0: count L
;.7=1: full
.ENDIF

