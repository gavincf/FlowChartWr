
CHIPNO 	EQU	'N589DXX0' ;N589XXX/N589DXX0 @N589 series


;C_GAPTIME	EQU	5
;	according to Fosc
;5: 5.33MS
;8: 8MS
;81: 8.192MS

.IF (CHIPSERIES = 'N567G') .OR. (CHIPSERIES = 'N567H') .OR. (CHIPSERIES = 'N567K') .OR. (CHIPSERIES = 'N588L')
C_CPU_CLOCK EQU 6144 ; KHZ; for N588H,N588J,N567G,N567H,N567K,N588L
C_SYS_CLOCK EQU C_CPU_CLOCK
.ENDIF
.IF (CHIPSERIES = 'N588H') .OR. (CHIPSERIES = 'N588J')
C_CPU_CLOCK EQU 6144 ; KHZ; for N588H,N588J,N567G,N567H,N567K,N588L
C_SYS_CLOCK EQU C_CPU_CLOCK
C_N588HJLVD_ENABLE EQU 0
.ENDIF
.IF (CHIPSERIES = 'N589A') .OR. (CHIPSERIES = 'N589B').OR. (CHIPSERIES = 'N589C').OR. (CHIPSERIES = 'N589D')
C_CPU_CLOCK EQU 20000 ; KHZ ;only for N589A,N589B,N589C,N589D ---------FCPU
C_SYS_CLOCK EQU 10000 ; KHZ ;only for N589A,N589B,N589C,N589D ---------FCPU
.ENDIF

External_MIDI_Note EQU	0
;=1 : N567H_V106083 IDE: support NOTE_ON,NOTE_OFF
C_SPCH1_ENABLE EQU	1
C_SPCH2_ENABLE EQU	0
C_MS1_ENABLE EQU	0
C_SPCH3_ENABLE EQU	0
C_EVO1_ENABLE	EQU	0 ; for check EVO1 END 
C_SPMD_ENABLE EQU	0

C_SPCH1_LP8	EQU	0
C_SPCH2_LP8	EQU	0
C_SPCH3_LP8	EQU	0
C_NOTEONOFF_EN	EQU	0

Speech_Volume_Adjustment EQU 0 ;for N588HJ FW  / N567H SW
C_MS1_GLOBALVOL_ENABLE	EQU	0 ; N567H SW

C_MS_SPCH1_MIX_EN 		EQU		0


C_AVAILABLE_RAMS EQU	447 ;rewrite it
C_RESERVE_BYTE_USER_STACK EQU 12; **pls rewrite it according your code request

C_MIDI_END_EVENT	EQU	0

C_BP2_EXIST	EQU	0 ; only for N588H,N588J,N567G,N567H,N567K,N588L,N589A,N589B,N589C,N589D
C_BP3_EXIST	EQU	0 ; only for N589A,N589B,N589C,N589D

;--------------------- min 1 port EN,min 1 enable
C_BP0_STATUS_EN	EQU	1; 
C_BP1_STATUS_EN	EQU	1;
.IF ( C_BP2_EXIST .EQ. 1)
C_BP2_STATUS_EN	EQU	1;
.ELSE
C_BP2_STATUS_EN	EQU	0;
.ENDIF
.IF ( C_BP3_EXIST .EQ. 1)
C_BP3_STATUS_EN	EQU	1;
.ELSE
C_BP3_STATUS_EN	EQU	0;
.ENDIF
;------------------------------------------

.IF (CHIPSERIES = 'N588H') .OR. (CHIPSERIES = 'N588J')
C_BP0M_USER_USED EQU 1
C_BP1M_USER_USED EQU 1
C_BP2M_USER_USED EQU 0
C_ULTRAIO_IO_EN	EQU	0 ; for N588HJ only
.ELSE
C_ULTRAIO_IO_EN	EQU	0 ; 
.ENDIF

.IF (CHIPSERIES = 'N589A') .OR. (CHIPSERIES = 'N589B').OR. (CHIPSERIES = 'N589C').OR. (CHIPSERIES = 'N589D')
C_PROTECTION_PAGE_EN EQU 0; for N589 write/read flash

C_N589_TOUCH_EN EQU	0
C_CAP_WAKEUP_EN EQU	0

;IDE.SDS option:
VDDSPI_FROM_LDO33 EQU 1;VDDSPI power source from LDO33 inside IC.
	;Set jumper option on T-board/EVB;enable LDO33 output in code.

.ENDIF

;set IO to High for preset IO berfore ultra IO initial
;for N588H,N588J,N567G,N567H,N567K,N588L,N589A,N589B,N589C,N589D
C_ULTRAIO_BP0	EQU	00000000B
C_ULTRAIO_BP1	EQU	00000000B
C_ULTRAIO_BP2	EQU	00000000B
C_ULTRAIO_BP3	EQU	00000000B



